package main

import (
	"_PROJECT_/client/internal"
	"github.com/gargous/flitter"
)

var cli flitter.Client

func init() {
	cli = flitter.NewClient(_ADDR_)
}

func main() {
	internal.OnStart(cli)
	internal.OnEnd(cli.Start())
}
